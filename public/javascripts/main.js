
// Sidenav for mobiles
const navs = document.querySelectorAll('.sidenav');
const instancesNav = M.Sidenav.init(navs);

// Modal frames
const modals = document.querySelectorAll('.modal');
const instancesModal = M.Modal.init(modals);
